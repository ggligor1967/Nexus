# Nexus v0.7 Promotion — Artifact Quality + Revision UX

Status: PROMOTED — MERGED TO MAIN, HOSTED CI GREEN

## Final status

```text
Nexus v0.5: RUNTIME VERIFIED — BASELINE COMMITTED
Nexus v0.6: HARDENED BASELINE VERIFIED
Nexus v0.7: PROMOTED — ARTIFACT QUALITY + REVISION UX
Branch: feature/v0.7-artifact-quality-revision-ux -> merged to main
Merge commit: 8492a8c (PR #1)
Hosted CI (main): GREEN
```

## Delivered scope

```text
1. Deterministic Markdown export improvements
   - pure planToMarkdown(name, plan, meta) renderer
   - YAML frontmatter with escaped values (yamlString)
   - persisted-only metadata (ai_plan_runs.completed_at / .model_name)
   - numbered sections, Constraints subsection, risk-level callout
   - exportablePlanMarkdown excluded from main output
2. Read-only revision history (model A: revision on each successful regeneration with a prior completed plan)
3. Hardened revisions write path (migration 0003: client INSERT/UPDATE removed; server/service-role writes only; non-fatal insert)
4. Regenerate UI (product-facing RegeneratePanel + prefilled concept intake)
5. Read-only revision snapshot route (/projects/[id]/revisions/[revisionId]; both snapshots via GeneratedPlanView readOnly; non-owner 404)
```

## Out of scope (confirmed absent)

```text
- restore of a previous revision
- PDF export
- diff engine
- market scan
- collaboration
- deployment expansion
```

## Commits (no WIP)

```text
3cb51c5 feat: improve deterministic markdown export
fdab768 feat: add read-only revision history
```

## Hosted CI evidence

```text
PR #1 run  28486550475 (pull_request): GREEN
  static-gates: PASS (npm ci, dep pin check, audit high/critical, typecheck, build)
  e2e-local-supabase: PASS (Supabase local, migrations incl. 0003, fixture app :3001, Playwright)
main  run 28543993912 (push, merge commit 8492a8c): GREEN
  static-gates: PASS
  e2e-local-supabase: PASS
```

## Test coverage added

```text
Slice 1: markdown-unit (purity + escaping), EXPORT-CONTENT (frontmatter/constraints/callout/numbered option/escaped title)
Slice 2: REV-SEC-001 (client revision insert denied), REV-001 (exactly one revision; snapshots differ), REV-002 (invalid_json writes none),
         REV-PAGE-001 (plan+panel survive failed regen with banner), REV-UI-001 (UI regenerate -> one revision; snapshot both versions read-only),
         REV-RLS-001 (user B 404 on user A snapshot route)
```

## Invariants preserved

```text
- validated plan_json as source of truth
- RLS isolation between users
- server-controlled artifact/write paths
- deterministic Markdown renderer (no time/randomness in the export path)
- typecheck / build / E2E / hosted GitHub Actions green before promotion
```

## Local gate caveat

```text
The run-together full E2E suite and `next build` were NOT run locally for the final
consolidated gate because the local host disk reached 100%. Hosted GitHub Actions
(clean runner) is the authoritative gate and passed both jobs on the PR and on main.
```

## Deferred (documented, non-blocking)

```text
- 10 Minor review findings triaged DEFER by the final whole-branch review (see PR #1 body).
  Notable: same-concept regeneration writes previous_snapshot === new_snapshot (by design, model A).
```

## Maintenance backlog (carried forward)

```text
CI-WARN-001: actions/checkout@v4 and actions/setup-node@v4 target Node.js 20 (forced onto Node 24 on the runner).
Non-blocking annotation on all runs. Revisit when a stable newer action major is standard.
```

## Next

```text
v0.8 candidate (deferred from v0.7): restore a previous revision; then evaluate PDF export once revision flow is stable.
```
