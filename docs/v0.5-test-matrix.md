# Nexus v0.5 Test Matrix

Status: COMPLETE IN LOCAL E2E, 8/8

## Auth

- AUTH-001: sign up succeeds.
- AUTH-002: login succeeds.
- AUTH-003: logout clears dashboard access.

## Projects

- PROJECT-001: logged-in user creates project.
- PROJECT-002: anonymous user cannot create project.

## Plan Generation

- PLAN-001: valid concept generates validated `plan_json`.
- PLAN-002: invalid concept under 50 characters is blocked before AI call.
- PLAN-003: invalid AI JSON is rejected and logged with `raw_output` when available.

## Export

- EXPORT-001: completed plan exports Markdown.
- EXPORT-002: export creates `exported_plans` row.

## v0.6 Coverage Closure

```text
TEST-001: CLOSED - full E2E matrix completion
TEST-002: CLOSED - invalid_json fail-closed E2E proof
TEST-003: CLOSED - exported_plans DB assertion
```

## RLS

- RLS-001: user A cannot list user B projects.
- RLS-002: user A cannot open user B project detail.
- RLS-003: user A cannot generate plan under user B project.
- RLS-004: user A cannot export user B plan.

## Safety

- SAFETY-001: critical-risk plan disables build-ready until acknowledgement.
- SAFETY-002: acknowledgement persists after refresh.

## v0.6 Security Hardening

- SEC-002: build-ready acknowledgement requires a completed validated plan.
- SEC-003: users can read own `ai_plan_runs` but cannot insert or update runs directly.

## Deterministic AI QA

Use this for normal deterministic flow:

```env
NEXUS_AI_PROVIDER=fixture
NEXUS_FIXTURE_SCENARIO=auto
```

Use this to force invalid AI JSON and verify failure logging:

```env
NEXUS_AI_PROVIDER=fixture
NEXUS_FIXTURE_SCENARIO=invalid_json
```
