# Nexus v0.6 Reproducibility Baseline

Status: HARDENED BASELINE VERIFIED

## Gate status

```text
Nexus v0.6: HARDENED BASELINE VERIFIED
CI reproducibility local gate: PASS WITH CAVEAT
LOCK-001: PASS WITH ENVIRONMENT CAVEAT

npm ci: PASS
typecheck: PASS
build: PASS
E2E: PASS, 8/8
E2E mode: local Playwright binary, workers=1
CI workflow: ADDED
Hosted CI: PASS
Hosted CI run: https://github.com/ggligor1967/Nexus/actions/runs/28331645298
Baseline commit: 0b0e148 - Add local reproducibility scripts
Repository: CLEAN
Commit required for LOCK-001: NO
```

## Interpretation

The local reproducibility gate is not blocked by the app, Supabase, RLS, or Playwright.

`NODE_OPTIONS` was empty in the shell session, so the original failure was not caused by a low explicit Node heap limit.

Observed behavior:

```text
npx playwright test --workers=1: BLOCKED by ENOSPC in C:\npm-cache
.\node_modules\.bin\playwright.cmd test --workers=1: PASS
npm run test:e2e:local with temporary npm_config_cache: PASS
```

## Windows local E2E caveat

If `npx` or `npm run` fails with `ENOSPC` in `C:\npm-cache`, use a temporary npm cache under `C:\tmp` or clear/relocate the global npm cache.

```powershell
$env:npm_config_cache='C:\tmp\npm-cache-nexus'
```

## Validated local E2E command

```powershell
$env:NODE_OPTIONS='--max-old-space-size=2048'
$env:E2E_BASE_URL='http://localhost:3001'
$env:npm_config_cache='C:\tmp\npm-cache-nexus'
npm run test:e2e:local
```

Result:

```text
E2E PASS, 8/8
```

## Product boundary

Product expansion remains forbidden for v0.6. The v0.6 track is limited to hardening, CI, RLS/service-role security, and test coverage.

## Next task order

```text
Closed:
- OPS-001: repair invalid Git metadata
- CI-LOCAL-001: local E2E reproducibility scripts
- LOCK-001: npm ci reproducibility baseline
- TECH-001: fix Next.js workspace root inference warning
- TECH-002: migrate middleware convention to proxy
- DEP-001: pin direct dependency specs
- SEC-001: npm audit risk accepted for Next/PostCSS moderate vulnerability
- TEST-001: complete E2E matrix
- TEST-002: invalid_json E2E proof
- TEST-003: exported_plans DB assertion
- SEC-002: guard build_ready_acknowledged behind valid completed plan
- SEC-003: restrict ai_plan_runs writes to server/service-role path
- CI-001A: add reproducible GitHub Actions baseline pipeline
- CI-001B: confirm hosted GitHub Actions run

Deferred:
- SEC-001: Next/PostCSS moderate vulnerability remediation deferred

Open:
- none
```

## CI-001 scope

```text
CI/CD report: ACCEPTED AS BLUEPRINT
v0.6 CI scope: one reproducible baseline workflow
Enterprise workflows: BACKLOG, not active

Implemented:
- .github/workflows/nexus-ci.yml
- static-gates job: npm ci, dependency pin check, audit high/critical, typecheck, build
- e2e-local-supabase job: Supabase local, migrations, fixture env, app on 3001, Playwright E2E
- failure artifacts: Next dev log, Playwright test results, Playwright report

Hosted verification:
- run: https://github.com/ggligor1967/Nexus/actions/runs/28331645298
- commit: d04dd6b9687a990742650a8fa5904c0b1dd8d262
- static-gates: PASS
- e2e-local-supabase: PASS
```

## Maintenance backlog

```text
CI-WARN-001: monitor/upgrade GitHub Actions runtime warnings

Scope:
- actions/checkout@v4
- actions/setup-node@v4

Decision:
- non-blocking for v0.6
- revisit when stable newer major/action guidance is available
```

## SEC-001 audit decision

```text
SEC-001: DEFERRED WITH RISK ACCEPTANCE

Exact vulnerabilities:
- postcss <8.5.10: GHSA-qx2v-qp2m-jg93, moderate, CWE-79, CVSS 6.1
- next: moderate via postcss, affected range 9.3.4-canary.0 - 16.3.0-canary.5

Dependency path:
next -> postcss

Forced fix:
Not applied. npm audit reports fix available only via npm audit fix --force,
which would install next@9.3.3 as a SemVer-major breaking/downgrade change.

Mitigation:
- direct dependency specs are pinned
- npm ci reproducibility is verified
- monitor compatible Next/PostCSS patch updates
- rerun npm ci, typecheck, build, and E2E before any dependency upgrade
```

## Final status

```text
Nexus v0.6: HARDENED BASELINE VERIFIED
Product expansion: STILL FORBIDDEN
Next task: v0.7 planning only
```
